# Set in the hosting provider (Vercel: Project > Settings > Environment Variables), or pass as a build arg to
# website/Dockerfile. Inlined at build time, so redeploy after changing it.
NEXT_PUBLIC_BACKEND_URL=https://api.voicematics.example.com
